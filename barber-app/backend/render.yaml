services:
  - type: web
    name: barber-backend
    runtime: node
    rootDir: barber-app/backend
    buildCommand: npm install --production=false
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: AUTO_SEED
        value: false
