services:
  - type: web
    name: barber-backend
    runtime: node
    rootDir: .
    buildCommand: npm install --production=false && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: AUTO_SEED
        value: false
