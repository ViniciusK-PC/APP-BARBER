services:
  - type: web
    name: barber-backend
    runtime: node
    rootDir: barber-app/backend
    buildCommand: npm install --production=false && rm -rf dist && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: AUTO_SEED
        value: false
